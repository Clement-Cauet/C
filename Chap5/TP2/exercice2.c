#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <limits.h>

int main(int argc, char *argv[]){
	FILE * source;
	source = fopen("exercice2.txt", "w+");
	int i;
	char ch, str[100];
	int getc(FILE * exercice2);
	
	printf("-- Lecture et ecriture dans un fichier --\n");
	gets(str);
	for(i=0; str[i]!='\0'; i++){
	}
	puts(str);
	
	getch();
	return 0;
}


